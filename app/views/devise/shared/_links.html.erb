<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= button_to "Inicia con Google", omniauth_authorize_path(resource_name, provider), class: 'btn btn-primary', data: { turbo: false } %>
  <% end %>
<% end %>